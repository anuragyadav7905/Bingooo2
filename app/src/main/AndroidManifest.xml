<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed to access internet resources -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bingooo"
        tools:targetApi="31"
        tools:ignore="ExtraText">

        android:icon="@mipmap/ic_launcher"


        <!-- Splash screen handles launcher and redirect -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">

            <!-- Launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Handle browser redirect (after Google login) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="bingooo.site"
                    android:path="/dashboard" />
            </intent-filter>
        </activity>

        <!-- WebView to play the game -->
        <activity
            android:name=".WebViewActivity"
            android:exported="true" />

    </application>
</manifest>
